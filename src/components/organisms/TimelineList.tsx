import React from 'react';
import { StyleSheet, View } from 'react-native';

export const TimelineList: React.FC = () => (
  <View style={styles.list}>
    {/* Example: <Card>Timeline Item</Card> */}
  </View>
);

const styles = StyleSheet.create({
  list: {
    marginVertical: 16,
  },
});
